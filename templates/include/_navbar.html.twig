<nav class="navbar navbar-expand-lg container">
	<div class="container-fluid">
		<a class="navbar-brand" href="/"><img class="navbar_logo" src="{{ asset('assets/images/leboncoin.png') }}" alt="Logo LeBonCoin"></a>

		<div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
			<a type="button" class="btn btn-outline-primary navbar_collapse_link me-5">
				<twig:ux:icon name="basil:add-outline"/>
				Déposer une annonce
			</a>
			<form class="d-flex justify-content-center search-form" role="search">
				<div class="position-relative">
					<input class="form-control" type="search" placeholder="Recherche sur leboncoin" aria-label="Search">
					<button class="btn btn-outline-success d-flex justify-content-center" type="submit">
						{{ ux_icon('iconamoon:search') }}
					</button>
				</div>
			</form>
		</div>

		<div class="d-flex flex-row-end align-items-center mt-3 menu-1">
			<ul class="d-flex">
				<li class="d-flex flex-column align-items-center me-4">
					{{ ux_icon('ph:bell-bold', {class: "navbar_icon"}) }}
					<a href="#" class="navbar_list_a">
						Mes recherches
					</a>
				</li>
				<li class="d-flex flex-column align-items-center me-4">
					{{ ux_icon('ph:heart-bold', {class: "navbar_icon"}) }}
					<a href="#" class="navbar_list_a">
						Favoris
					</a>
				</li>
				<li class="d-flex flex-column align-items-center me-4">
					{{ ux_icon('tabler:message', {class: "navbar_icon"}) }}
					<a href="#" class="navbar_list_a">
						Messages
					</a>
				</li>
				{% if app.user  %}
					<li class="d-flex flex-column align-items-center">
						<a href="{{path('account_index')}}" class="">
							{{app.user.username}}
						</a>
					</li>
				{% else %}
					<li class="d-flex flex-column align-items-center">
						{{ ux_icon('ph:user-bold', {class: "navbar_icon"}) }}
						<a href="/login" class="navbar_list_a">
							Se connecter
						</a>
					</li>
				{% endif %}
			</ul>
		</div>

	</nav>
	<twig:NavBar/>
	<div class="divider"></div>
